const add             = (a,b)=> a+b;
const subtract        = (a,b)=> a-b;
const mutiply         = (a,b)=> a*b;
const divide          = (a,b)=> a/b;

const firstVariable   = 0 ;
const secondVariavle  = 0 ;
const operator        = null ;

const operate = (operator,num1,num2)=>{
    add(num1,num2);
    subtract(num1,num2);
    mutiply(num1,num2);
    divide(num1,num2);
};